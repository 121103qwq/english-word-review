# GitHub 推送失败原因与修复总结

日期：2026-08-07

## 问题现象

在当前项目执行 `git push` 时，多次出现：

```text
Recv failure: Connection was reset
```

GitHub 登录状态和仓库权限均正常，提交及大文件也能够被 GitHub 接收，因此问题不是账号、令牌、分支权限或文件大小硬限制。

## 原因

- GitHub 远端使用 HTTPS：`https://github.com/121103qwq/english-word-review.git`。
- 本机代理在 `127.0.0.1:7892` 正常监听。
- 环境中只有 `HTTP_PROXY=http://127.0.0.1:7892`，没有 `HTTPS_PROXY`。
- Git 原先也没有自己的代理配置。
- 结果是 HTTPS Git 推送没有稳定经过 7892，直连 GitHub 时连接被重置。
- 显式指定 `http://127.0.0.1:7892` 后，同一分支成功上传，证明代理路由缺失是直接原因。

## 已实施的修复

已为当前 Git 仓库写入本地代理配置：

```powershell
git config --local http.proxy http://127.0.0.1:7892
```

这是仓库级配置，不是系统全局配置，不会影响其他独立 Git 仓库。当前目录是同一仓库的工作树，因此配置实际保存在公共 Git 目录的 `.git/config` 中，并对该仓库的其他工作树生效。

配置中只保存代理地址，没有保存 GitHub 令牌、账号密码或其他凭据。

## 验证结果

以下检查均已成功：

```powershell
git ls-remote origin HEAD
git push --dry-run origin codex/fix-startup-reload-loop
```

验证结果：

- 能够读取远端 `HEAD`。
- 推送演练返回 `Everything up-to-date`。
- 实际分支 `codex/fix-startup-reload-loop` 已成功上传。
- 草稿 PR：<https://github.com/121103qwq/english-word-review/pull/1>

## 后续使用

正常推送可直接执行：

```powershell
git push
```

代理程序必须正在运行，并继续监听 7892。如果以后代理端口改变，例如改为 7890，可更新配置：

```powershell
git config --local http.proxy http://127.0.0.1:7890
```

如果以后不再使用代理，可撤销本项目配置：

```powershell
git config --local --unset http.proxy
```

查看当前配置：

```powershell
git config --local --show-origin --get http.proxy
```

## 相关说明

- GitHub 对本次约 52.57 MiB 的 Android APK 给出了“大于建议的 50 MiB”警告，但仍成功接收；它不是此前连接重置的原因。
- 如果未来发布文件继续增大并接近 GitHub 单文件 100 MiB 硬限制，应把安装包放入 GitHub Release，或采用 Git LFS，而不是继续直接提交到普通 Git 历史。
